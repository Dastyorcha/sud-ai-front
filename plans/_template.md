# NNN — <Title>

- **Status:** idea | doing | done
- **Size:** medium | large
- **Author model:** Opus 4.8 (planner)

## Goal

One sentence: what this delivers and why.

## Scope & non-goals

- **In scope:** …
- **Out of scope:** …

## Affected files

| Path (FSD layer)                      | New? | Intent              |
| ------------------------------------- | ---- | ------------------- |
| `src/views/…`                         | yes  | …                   |
| `src/widgets/…`                       | no   | …                   |
| `src/shared/constants/route-paths.ts` | no   | add `ROUTE_PATHS.…` |

## Steps

Each step is one committable sub-task (`type: message`).

1. [ ] … — `feat: …`
2. [ ] … — `feat: …`
3. [ ] docs: sync `docs/codemap.md` — `docs: …`

## Risks / ripple / escalation

- Shared surface touched: …
- New dependency: none | `<name>` (needs user approval)
- Escalation points: …
- Rollback: …

## Verification

- `npx tsc -b` clean, `npm run lint` clean.
- `/tools` verified in light + dark (if visual).
- Manual flow: …
