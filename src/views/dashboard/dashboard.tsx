import { ComingSoon } from "@/shared/custom/coming-soon";

/** Dashboard placeholder — the real widgets (KPIs, charts, activity) land in
 * a later plan. */
export default function Dashboard() {
  return <ComingSoon />;
}
