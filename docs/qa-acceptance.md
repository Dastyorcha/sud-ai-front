# QA / acceptance

Acceptance checklists run at the end of a plan or before a release. Two
tracks live here:

- **TZ §27** (LexKotib_AI_MVP_Texnik_Spetsifikatsiya.md) — feature-coverage/
  a11y/known-gaps checklist against the original product spec. Not yet
  formally run against this codebase; run and record here when a plan
  explicitly targets it.
- **Integration guide §18** — the frontend acceptance checklist for the real
  LexKotib backend integration (`plans/done/integration-*.md`), run as a
  code-level audit (no live backend in this environment) at the end of
  integration-11. See below.

## Integration guide §18 — frontend acceptance (code-level audit)

Verified by reading the source, not by exercising a live backend — each row
cites the exact file:line evidence. Re-run against a live backend before
shipping.

| #   | Check                                                                               | Result | Evidence                                                                                                                                                                                                                                                                                                                                                         |
| --- | ----------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Bearer token attached on every protected request                                    | PASS   | `src/shared/lib/http/api-client.ts:52-55` — request interceptor sets `Authorization: Bearer <token>` from the injected getter on every call                                                                                                                                                                                                                      |
| 2   | `X-Request-ID` sent on every request, logged/captured on response                   | PASS   | `src/shared/lib/http/api-client.ts:50` (request), `:62`/`:67` (response, both success and error paths capture it via `setLastRequestId`)                                                                                                                                                                                                                         |
| 3   | Token refresh is single-flight (no duplicate concurrent refresh calls)              | PASS   | `src/shared/lib/auth/refresh-manager.ts:23-58` — `refreshSession()` shares one `refreshPromise` across concurrent `401`s                                                                                                                                                                                                                                         |
| 4   | `401` vs `403` vs ProblemDetails vs ValidationProblemDetails are distinguished      | PASS   | `src/shared/lib/http/api-error.ts:119-147` — `isValidationProblem` branch (→ `fieldErrors`), `body.code` branch, status-fallback branch (`401→unauthorized`, `403→forbidden`)                                                                                                                                                                                    |
| 5   | Role + case-level access enforced (frontend gate, backend is real enforcement)      | PASS   | `src/shared/constants/court-permissions.ts` (`canCourt`/`COURT_CAPABILITIES`) gates UI actions by the real `/auth/me` role; `case_access_denied`/`forbidden` error codes are mapped and toasted (`src/shared/lib/errors/error-map.ts`) rather than silently failing                                                                                              |
| 6   | Latest `expectedVersion` sent on every versioned mutation                           | PASS   | Transcript segment edit/verify (`src/features/transcript/transcript.service.ts:34,57`), transcript approve (`:152-156`), event edit/verify (`src/features/events/event.service.ts:118,140`), every document lifecycle mutation (`src/features/documents/document.service.ts:93-234`) — each takes/sends `expectedVersion` from the caller's last-known `version` |
| 7   | `FormData` uploads never set `Content-Type` manually                                | PASS   | `src/features/hearings/hearing.service.ts:133-141` (audio upload) and `src/features/documents/template.service.ts:88` build a `FormData` body and let axios/the browser derive the multipart boundary — no manual header set                                                                                                                                     |
| 8   | Job polling stops at a terminal state                                               | PASS   | `src/shared/lib/query/use-job-polling.ts:23,54` — `TERMINAL_STATUSES = {Succeeded, Failed}`; `refetchInterval` returns `false` once reached                                                                                                                                                                                                                      |
| 9   | Blob downloads bypass the JSON error interceptor                                    | PASS   | `src/features/documents/document.service.ts:287-301` — `downloadDocument` requests `responseType: "blob"`, parses a non-2xx body manually via `parseBlobError` instead of the JSON `parseApiError` path                                                                                                                                                          |
| 10  | `storageKey`/`pdfStorageKey` never rendered as a URL                                | PASS   | Grepped every `.tsx` for `storageKey`/`pdfStorageKey` — only referenced in service/type files (`document.service.ts`, `template.service.ts`, `models.ts`), never interpolated into an `<a href>`/`<img src>`/similar                                                                                                                                             |
| 11  | Validate gates approve (transcript)                                                 | PASS   | `src/features/transcript/real-transcript-panel.tsx:111` — `canApprove = !locked && hasVersion && validation?.isValid === true`, `disabled={!canApprove                                                                                                                                                                                                           |     | ...}` on the approve button |
| 12  | Event/document source refs (`sourceSegmentIds`/`sources[]`) preserved through edits | PASS   | `src/features/events/event.service.ts:114` (edit doesn't touch `sourceSegmentIds`), `sources` shown/preserved in `events-panel.tsx:188-190`; document `contentJson` fields/sections/paragraphs round-trip every `sources[]` verbatim per `src/features/protocol/protocol-panel.tsx`'s doc comment and `models.ts`'s `DocumentContent` shape                      |
| 13  | Approved transcript/document UIs go read-only                                       | PASS   | `src/features/transcript/real-transcript-panel.tsx:109` (`locked = hearing.status === "Approved"`), `src/features/protocol/protocol-panel.tsx:204-209` (`isEditable`/`canReview`/`canExport` gated by `doc.status`)                                                                                                                                              |
| 14  | No real PII in demo/fixture data                                                    | PASS   | Mock fixtures (`src/shared/lib/mock-api/data/*`) use invented Uzbek names/orgs/case numbers consistent with the existing mockup dataset; no real passport/TIN/phone numbers — spot-checked `court-users.ts`, `participants.ts`, `court-cases.ts`                                                                                                                 |

All 14 items pass on a code-level read. Item 5's "real enforcement" and every
network-dependent item (1–3, 6–9) should be re-verified against a live
backend once one is reachable in this environment — this audit only confirms
the frontend _sends_/_handles_ the contract correctly, not that the backend
honors it.
